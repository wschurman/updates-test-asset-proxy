# Example proxy for EAS Update assets

- `staging-assets.eascdn.net/<asset_key>` is proxied by `./app/[assetkey]+api.tsx` (would need to update to just be `assets.eascdn.net` for production EAS Update)